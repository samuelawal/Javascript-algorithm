// Create a function that accepts two numbers
function myTwoNumbers(myNum, num){
  // Store the minimum number in a variable
  const min = Math.min(myNum , num)
  // Store the maximum number in a variable
  const max = Math.max(myNum, num)
  // Create a conditional statement that states which number is larger
    if(max > min){
  return ( `${max} is larger than ${min}`)
    }
    else {
    cre
    }
}
console.log(myTwoNumbers(100, 100))